{
	i = 1;

  fn double(a) {
    a = a + a;
  }

  // This should trigger an error, 
  // because this symbol is unreachable.
  call triple(b);

  v = 2;

  {

  	w = 3;
    
  	fn triple(b) {
      b = b + b + b;
    }
    
  	z = 4;
  }
}